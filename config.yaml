# Arista Demo Dashboard Configuration
# URLs, tokens, and SSH credentials are configured in the browser UI.

server:
  port: 3000

environments:
  dev:
    label: "DEV / TEST"
  prod:
    label: "PRODUCTION"
